from django.urls import path;from rest_framework.routers import DefaultRouter;from .views import NewsSearchViewSet;app_name = "news_search";router = DefaultRouter(trailing_slash=False);router.include_format_suffixes = False;router.register("", NewsSearchViewSet, basename="news-search");urlpatterns = router.urls
